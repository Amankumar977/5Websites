/**
 * The ternary operator is used to check when we have to use mulitiple operands like if we have to check that whether the number is less than or 50 or not than we can use ternary operator.
 */
let number = 78;
console.log(number % 2 === 0 ? "even" : "odd");
